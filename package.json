{
  "name": "bitwebcore-site",
  "type": "module",
  "version": "1.0.1",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --port=8080",
    "build:clean": "node contrib/doc-gen/clean-rpc-docs.js && eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-navigation": "^1.0.5"
  },
  "overrides": {
    "js-yaml": "^4.2.0"
  }
}
